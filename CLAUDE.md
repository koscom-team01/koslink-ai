# Claude Code 세션 지침 — 토큰 절약

- 답변은 간결하게. 요청받지 않은 배경 설명, 요약 재진술, 장황한 서론/결론을 붙이지 않는다.
- 파일을 읽을 때는 먼저 Grep/Glob으로 범위를 좁힌 뒤, 필요한 부분만 `offset`/`limit`으로 읽는다. 이미 대화에서 본 파일 내용을 다시 통째로 인용하지 않는다.
- 파일 수정은 전체 재작성(Write)보다 최소 diff(Edit)를 우선한다. Edit/Write 직후 결과 확인을 위해 파일을 다시 읽지 않는다 (도구가 실패하면 에러로 알려준다).
- 서로 의존하지 않는 조회/조사 작업은 한 메시지에서 병렬로 실행한다.
- 방대한 탐색·조사가 필요한 하위 작업은 fork/subagent로 위임해 원본 대화 컨텍스트를 가볍게 유지한다.
- 커맨드 출력이 큰 경우 grep/head/tail 등으로 필요한 부분만 걸러서 확인한다.
